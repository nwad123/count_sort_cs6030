#pragma once

/// Serial count sort implementation provided by the professor
///
/// `a` input array to sort 
/// `n` length of `a`
///
/// Program sorts `a[]` in-place and uses `n * 4` temporary storage
void count_sort_reference(int a[], int n);
